# Secrets at any folder depth (explicit `**`): backend/.env, frontend/.env, etc.
# Tracked exceptions: anywhere named `.env.example` (e.g. repo root).
**/.env
**/.env.*
!**/.env.example

# Generated app data (see specs/tech-stack.md)
/data/kirana.db
/data/kirana.db-journal
/data/kirana.db-wal
/data/kirana.db-shm
/data/conversations.csv

# OS / editor (when not scoped by backend or frontend cwd)
.DS_Store
Thumbs.db
*.sw?

.claude